import React from 'react';
import { Card } from 'antd';

const ContentReview = () => {
    return (
        <Card title="Revisión de Contenido">
            <p>Componente de revisión de contenido</p>
        </Card>
    );
};

export default ContentReview;